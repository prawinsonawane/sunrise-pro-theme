{{ 'sunrise-collections-header.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .sunrise-collections-header {
    text-align: center;
    padding: 3rem 0;
    background: linear-gradient(135deg, var(--color-background) 0%, var(--color-background-secondary) 100%);
  }
  
  .sunrise-collections-header__container {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 2rem;
  }
  
  .sunrise-collections-header__title {
    font-size: 3rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 1rem;
    line-height: 1.2;
  }
  
  .sunrise-collections-header__subtitle {
    font-size: 1.125rem;
    color: var(--color-text-muted);
    line-height: 1.6;
  }
  
  @media (max-width: 768px) {
    .sunrise-collections-header {
      padding: 2rem 0;
    }
    
    .sunrise-collections-header__title {
      font-size: 2rem;
    }
    
    .sunrise-collections-header__subtitle {
      font-size: 1rem;
    }
    
    .sunrise-collections-header__container {
      padding: 0 1rem;
    }
  }
{%- endstyle -%}

<div class="sunrise-collections-header">
  <div class="sunrise-collections-header__container">
    <h1 class="sunrise-collections-header__title">{{ section.settings.heading | default: 'All Collections' }}</h1>
    
    {%- if section.settings.subheading != blank -%}
      <p class="sunrise-collections-header__subtitle">{{ section.settings.subheading }}</p>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Collections Header",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Collections Header Settings"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "All Collections"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Browse our complete range of products"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding Top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding Bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 40
    }
  ]
}
{% endschema %}
